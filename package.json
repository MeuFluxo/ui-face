{
  "name": "ui-face",
  "version": "1.0.0",
  "private": true,
  "description": "Página estática — Ultra Inteligência",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check config.js && node --check script.js && node --check player.js && npm test"
  }
}
